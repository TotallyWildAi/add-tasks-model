# add-tasks-model
A Next.js TypeScript app with Prisma: add a Tasks model (id, title, done, createdAt) with GET /api/tasks (returns open tasks first) and POST /api/tasks (validates title is non-empty). Server Component on / lists open tasks. Add one Vitest test.
